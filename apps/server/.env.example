PORT=3001
HOST=0.0.0.0
# Explicitly enables the camera-free test room option; defaults off in production.
ALLOW_DEBUG_IDENTITY=false
# Comma-separated browser origins for a separate frontend deployment.
CLIENT_ORIGINS=http://localhost:5180,http://127.0.0.1:5180
